FROM mysql:8

# Files in /docker-entrypoint-initdb.d run automatically the first time
# the container starts with an empty data directory (i.e. a fresh disk).
COPY sql/schema.sql /docker-entrypoint-initdb.d/schema.sql
